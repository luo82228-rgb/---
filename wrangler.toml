name = "yp"
main = "src/index.ts"
compatibility_date = "2024-09-09"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./public"
binding = "ASSETS"
# 所有请求先进 Worker 做密码校验，否则静态资源会绕过登录直接命中
run_worker_first = true
